"use strict" // modo estrito. Sem ele, se eu não definir a variável nome com let, dará erro no programa (erro de referência).

let nome = 'Cristiano'
let curso = 'CFB Cursos'
console.log(nome)
console.log(`Boa tarde, ${nome}, bem-vindo ao ${curso}.`)